# Eggy

it's a task manager app thing that i made one day.

## How to run
`python main.py`

Or if you want to turn it into an executable:
`pyinstaller --clean --noconsole --onefile --windowed --icon=assets/egg_icon.ico --add-data "assets;assets" main.py`
